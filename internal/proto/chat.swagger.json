{
  "swagger": "2.0",
  "info": {
    "title": "chat.proto",
    "version": "version not set"
  },
  "tags": [
    {
      "name": "ChatService"
    }
  ],
  "consumes": [
    "application/json"
  ],
  "produces": [
    "application/json"
  ],
  "paths": {
    "/login": {
      "post": {
        "summary": "LogIn to the chatroom",
        "description": "LogIn to the chatroom, it takes username and returns a JWT token",
        "operationId": "ChatService_LogIn",
        "responses": {
          "200": {
            "description": "A successful response.",
            "schema": {
              "$ref": "#/definitions/chatLoginResp"
            }
          },
          "default": {
            "description": "An unexpected error response.",
            "schema": {
              "$ref": "#/definitions/rpcStatus"
            }
          }
        },
        "parameters": [
          {
            "name": "body",
            "description": "LoginReq is the request message for LogIn, include a not empty username.",
            "in": "body",
            "required": true,
            "schema": {
              "$ref": "#/definitions/chatLoginReq"
            }
          }
        ],
        "tags": [
          "ChatService"
        ]
      }
    }
  },
  "definitions": {
    "chatEmpty": {
      "type": "object"
    },
    "chatLoginReq": {
      "type": "object",
      "example": {
        "username": "Alice"
      },
      "properties": {
        "username": {
          "type": "string"
        }
      },
      "description": "LoginReq is the request message for LogIn, include a not empty username.",
      "title": "LoginReq",
      "required": [
        "username"
      ]
    },
    "chatLoginResp": {
      "type": "object",
      "properties": {
        "token": {
          "type": "string",
          "description": "The JWT token, encoded username in it"
        }
      },
      "description": "LoginResp is the response message for LogIn, include a not empty JWT token which encoded username in it.",
      "required": [
        "token"
      ]
    },
    "chatMessage": {
      "type": "object",
      "properties": {
        "text": {
          "type": "string",
          "description": "The text of the message, include username"
        },
        "timestamp": {
          "type": "string",
          "format": "int64",
          "description": "Timestamp when the user sent the message locally"
        }
      },
      "description": "Chat room message, include timestamp and text.",
      "required": [
        "timestamp",
        "text"
      ]
    },
    "protobufAny": {
      "type": "object",
      "properties": {
        "@type": {
          "type": "string"
        }
      },
      "additionalProperties": {}
    },
    "rpcStatus": {
      "type": "object",
      "properties": {
        "code": {
          "type": "integer",
          "format": "int32"
        },
        "message": {
          "type": "string"
        },
        "details": {
          "type": "array",
          "items": {
            "type": "object",
            "$ref": "#/definitions/protobufAny"
          }
        }
      }
    }
  }
}
